# used in
# https://gitlab.com/redhat/centos-stream/rpms/clevis-pin-trustee
summary: test plan for clevis gating testing

prepare:
  - how: shell
    script:
     - systemctl disable --now dnf-makecache.service || true
     - systemctl disable --now dnf-makecache.timer || true
     - dnf makecache

discover:
  - name: Upstream_tests_clevis_pin_trustee
    how: fmf
    url: https://github.com/RedHat-SP-Security/trustee-tests
    filter: tag:CI-Tier-1

execute:
    how: tmt
